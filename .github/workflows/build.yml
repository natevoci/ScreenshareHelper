name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:

    runs-on: windows-latest
    defaults:
      run:
        working-directory: ./src

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Test
      run: dotnet test --no-build --verbosity normal

    - name: Publish application
      run: dotnet publish -c Release -o ./Publish

    - name: Zip Release
      uses: thedoctor0/zip-release@master
      with:
        type: 'zip'
        filename: 'ScreenshareHelper-release.zip'
        directory: './src/Publish'
        exclusions: '*.deps.json *.pdb'

    - name: Create or Update tag
      run: |
        git tag -f ${{ github.ref }}Build
        git push -f origin ${{ github.ref }}Build

    - name: Upload Release
      uses: ncipollo/release-action@v1
      with:
        name: Release ${{ github.ref }}
        tag: ${{ github.ref }}Build
        allowUpdates: true
        prerelease: false
        artifacts: './src/Publish/ScreenshareHelper-release.zip'
        token: ${{ secrets.GITHUB_TOKEN }}
